@startuml
start

:Update pose;

if (hardware skinning) then (yes)
	:send pose data to the gpu;
	:calculate new vertex positions on the GPU via GLSL vertex shader;
else (no)
	:copy mesh data;
	:calculate new vertex positions on the CPU;
	:upload new mesh data to the GPU;
endif

stop

@enduml
